volumes:
  - name: kafka-cert
    secret:
    secretName: tls-chainindexing-worker.dev.astranet.services
volumeMounts:
  - name: kafka-cert
    mountPath: /certs/chainindexing.kafka.dev
specAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "8081"
serviceAccount: astra-indexing